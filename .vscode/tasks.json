{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "/usr/share/dotnet/dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Server/Server.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}